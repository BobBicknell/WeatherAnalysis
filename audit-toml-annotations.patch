diff --git a/cleanup_data/.cargo/audit.toml b/cleanup_data/.cargo/audit.toml
index 83b0e62..9f6cbc4 100644
--- a/cleanup_data/.cargo/audit.toml
+++ b/cleanup_data/.cargo/audit.toml
@@ -1,12 +1,23 @@
 # cargo-audit overrides.
 #
 # These advisories are all transitive through polars (fast-float, pyo3,
-# quick-xml), which pins those crates to versions cargo can't move past without
-# upgrading polars itself. No fixed version is reachable in the current
-# dependency tree, and none of them affect code paths this program uses.
+# quick-xml), which pins those crates to versions cargo can't move past
+# without upgrading polars itself. No fixed version is reachable in the
+# current dependency tree, and none of them affect code paths this program
+# uses.
 #
-# TODO: re-check after upgrading polars to a version with
-# pyo3 >= 0.29 / quick-xml >= 0.41; remove entries that no longer resolve.
+# Verified 2026-08-30 against current polars (locked at 0.44.2, pinning
+# pyo3 0.21.2 and quick-xml 0.36.2):
+#   - RUSTSEC-2025-0003 (fast-float): still no upstream fix at all.
+#   - RUSTSEC-2025-0020 (pyo3):            fixed in pyo3 0.24.1
+#   - RUSTSEC-2026-0177 (pyo3):            fixed in pyo3 0.29.0
+#   - RUSTSEC-2026-0194/0195 (quick-xml):  fixed in quick-xml 0.41.0
+#
+# polars' own pyo3-polars companion crate is now on pyo3 ^0.27, so a newer
+# polars release likely clears the pyo3 entries (quick-xml 0.41 needs
+# checking separately). Bumping polars 0.44 -> latest is a real upgrade
+# (likely breaking changes in the lazy-frame surface data-core uses) --
+# do it as its own task with a build+test pass, not a quick version bump.
 
 [advisories]
 ignore = [
diff --git a/plot_data/data-core/.cargo/audit.toml b/plot_data/data-core/.cargo/audit.toml
index 83b0e62..9f6cbc4 100644
--- a/plot_data/data-core/.cargo/audit.toml
+++ b/plot_data/data-core/.cargo/audit.toml
@@ -1,12 +1,23 @@
 # cargo-audit overrides.
 #
 # These advisories are all transitive through polars (fast-float, pyo3,
-# quick-xml), which pins those crates to versions cargo can't move past without
-# upgrading polars itself. No fixed version is reachable in the current
-# dependency tree, and none of them affect code paths this program uses.
+# quick-xml), which pins those crates to versions cargo can't move past
+# without upgrading polars itself. No fixed version is reachable in the
+# current dependency tree, and none of them affect code paths this program
+# uses.
 #
-# TODO: re-check after upgrading polars to a version with
-# pyo3 >= 0.29 / quick-xml >= 0.41; remove entries that no longer resolve.
+# Verified 2026-08-30 against current polars (locked at 0.44.2, pinning
+# pyo3 0.21.2 and quick-xml 0.36.2):
+#   - RUSTSEC-2025-0003 (fast-float): still no upstream fix at all.
+#   - RUSTSEC-2025-0020 (pyo3):            fixed in pyo3 0.24.1
+#   - RUSTSEC-2026-0177 (pyo3):            fixed in pyo3 0.29.0
+#   - RUSTSEC-2026-0194/0195 (quick-xml):  fixed in quick-xml 0.41.0
+#
+# polars' own pyo3-polars companion crate is now on pyo3 ^0.27, so a newer
+# polars release likely clears the pyo3 entries (quick-xml 0.41 needs
+# checking separately). Bumping polars 0.44 -> latest is a real upgrade
+# (likely breaking changes in the lazy-frame surface data-core uses) --
+# do it as its own task with a build+test pass, not a quick version bump.
 
 [advisories]
 ignore = [
diff --git a/plot_data/server/.cargo/audit.toml b/plot_data/server/.cargo/audit.toml
index 83b0e62..9f6cbc4 100644
--- a/plot_data/server/.cargo/audit.toml
+++ b/plot_data/server/.cargo/audit.toml
@@ -1,12 +1,23 @@
 # cargo-audit overrides.
 #
 # These advisories are all transitive through polars (fast-float, pyo3,
-# quick-xml), which pins those crates to versions cargo can't move past without
-# upgrading polars itself. No fixed version is reachable in the current
-# dependency tree, and none of them affect code paths this program uses.
+# quick-xml), which pins those crates to versions cargo can't move past
+# without upgrading polars itself. No fixed version is reachable in the
+# current dependency tree, and none of them affect code paths this program
+# uses.
 #
-# TODO: re-check after upgrading polars to a version with
-# pyo3 >= 0.29 / quick-xml >= 0.41; remove entries that no longer resolve.
+# Verified 2026-08-30 against current polars (locked at 0.44.2, pinning
+# pyo3 0.21.2 and quick-xml 0.36.2):
+#   - RUSTSEC-2025-0003 (fast-float): still no upstream fix at all.
+#   - RUSTSEC-2025-0020 (pyo3):            fixed in pyo3 0.24.1
+#   - RUSTSEC-2026-0177 (pyo3):            fixed in pyo3 0.29.0
+#   - RUSTSEC-2026-0194/0195 (quick-xml):  fixed in quick-xml 0.41.0
+#
+# polars' own pyo3-polars companion crate is now on pyo3 ^0.27, so a newer
+# polars release likely clears the pyo3 entries (quick-xml 0.41 needs
+# checking separately). Bumping polars 0.44 -> latest is a real upgrade
+# (likely breaking changes in the lazy-frame surface data-core uses) --
+# do it as its own task with a build+test pass, not a quick version bump.
 
 [advisories]
 ignore = [
diff --git a/plot_data/src-tauri/.cargo/audit.toml b/plot_data/src-tauri/.cargo/audit.toml
index 83b0e62..9f6cbc4 100644
--- a/plot_data/src-tauri/.cargo/audit.toml
+++ b/plot_data/src-tauri/.cargo/audit.toml
@@ -1,12 +1,23 @@
 # cargo-audit overrides.
 #
 # These advisories are all transitive through polars (fast-float, pyo3,
-# quick-xml), which pins those crates to versions cargo can't move past without
-# upgrading polars itself. No fixed version is reachable in the current
-# dependency tree, and none of them affect code paths this program uses.
+# quick-xml), which pins those crates to versions cargo can't move past
+# without upgrading polars itself. No fixed version is reachable in the
+# current dependency tree, and none of them affect code paths this program
+# uses.
 #
-# TODO: re-check after upgrading polars to a version with
-# pyo3 >= 0.29 / quick-xml >= 0.41; remove entries that no longer resolve.
+# Verified 2026-08-30 against current polars (locked at 0.44.2, pinning
+# pyo3 0.21.2 and quick-xml 0.36.2):
+#   - RUSTSEC-2025-0003 (fast-float): still no upstream fix at all.
+#   - RUSTSEC-2025-0020 (pyo3):            fixed in pyo3 0.24.1
+#   - RUSTSEC-2026-0177 (pyo3):            fixed in pyo3 0.29.0
+#   - RUSTSEC-2026-0194/0195 (quick-xml):  fixed in quick-xml 0.41.0
+#
+# polars' own pyo3-polars companion crate is now on pyo3 ^0.27, so a newer
+# polars release likely clears the pyo3 entries (quick-xml 0.41 needs
+# checking separately). Bumping polars 0.44 -> latest is a real upgrade
+# (likely breaking changes in the lazy-frame surface data-core uses) --
+# do it as its own task with a build+test pass, not a quick version bump.
 
 [advisories]
 ignore = [
